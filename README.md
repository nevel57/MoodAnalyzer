Russian Sentiment Analysis System (RuSentiment Hybrid)
Интеллектуальная система анализа тональности текстов на русском языке, сочетающая классическое машинное обучение и гибридную архитектуру для достижения баланса между скоростью работы и точностью.

🚀 Основные возможности
Гибридная маршрутизация (Smart Routing): Система автоматически анализирует сложность входящего текста и выбирает между «быстрой» моделью (TF-IDF + Logistic Regression) и «точной» (BERT) для оптимизации ресурсов.

Множественные профили моделей: Поддержка различных версий обученных моделей (Fresh, Combined, Optimized, Quality).

Полнофункциональный API: Построен на базе FastAPI с поддержкой одиночных запросов и пакетной обработки (batch processing).

Интерактивный UI: Встроенный веб-интерфейс для тестирования моделей в реальном времени и оценки их точности.

Инструменты обучения: Встроенные скрипты для переобучения моделей на новых данных (RuSentiment, RuReviews).

🛠 Технологический стек
Backend: Python 3.9+, FastAPI, Uvicorn.

ML Core: Scikit-learn (Logistic Regression, TF-IDF), Joblib, NumPy, Pandas.

Frontend: HTML5, CSS3 (Modern UI), JavaScript (Fetch API).

Data: Поддержка датасетов RuSentiment и отзывов о товарах.

🏗 Архитектура проекта
Система состоит из нескольких ключевых компонентов:

Request Router: Анализирует текст на наличие сарказма, длины и лингвистических особенностей, принимая решение о выборе модели.

Predictors: Модули RuSentimentPredictor и QualityPredictor, инкапсулирующие логику предсказания и вычисления уверенности (confidence).

Hybrid Service: Оркестратор, который связывает логику моделей и маршрутизацию в единый сервис.

📋 Доступные модели
В системе реализованы следующие конфигурации в MODEL_PATHS:

rusentiment_fresh: Обучена на чистом наборе данных RuSentiment.

rusentiment_combined: Объединенные данные социальных сетей и отзывов.

rusentiment_optimized: Оптимизированная версия для высокой производительности.

quality_sentiment: Специализированная модель для оценки качества отзывов.

🏁 Быстрый старт
1. Установка зависимостей
Bash

pip install fastapi uvicorn scikit-learn pandas numpy joblib
2. Запуск сервера
Bash

python app/run_rusentiment.py
После запуска API будет доступно по адресу: http://localhost:8000. Интерактивная документация (Swagger): http://localhost:8000/docs.

3. Тестирование через UI
Откройте файл sentiment_test.html в любом браузере для доступа к графическому интерфейсу тестирования.

🧬 API Endpoints
POST /analyze: Анализ одного текста. Принимает JSON с полем text и необязательным model.

POST /batch_analyze: Анализ списка текстов.

POST /train: Запуск процесса обучения модели на локальных датасетах.

GET /health: Проверка состояния сервиса.
